- Constants and "other things"
& Structs
* Functions
// Comments

src/main
 main.go
	
src/def
 def.go
	- Motor directions (UP = 1, DOWN = -1, STOP = 0)
	- Elevator constants (FLOORS = 4, ELEVATORS = 3)

src/fsm
 fsm.go

src/IO
 channels.go

 constants.go

 hw.go
	- type MotorDirection(MD_up = 1, MD_Down = -1, MD_Stop = 0)
	- type ButtonType(BT_HallUp = 0, BT_HallDown = 1, BT_Cab = 2)

	& ButtonEvent{
	Floor int
	Button ButtonType
	}

	//init function
	* initHw(addr strin, numFloors int)

	// set functions
	* setElevLights()
	* setDoorLightValue(value bool)
	* setFloorIndicatorLight(floor int)
	* setButtonLight(button ButtonType, floor int, value bool)
	* setMotorDir(dir MotorDirection)

	// get functions
	* getFloor()
		return int floor
	* getButton(button ButtonType, floor int)
		return button???

	// poll functions
	* pollFloors(reciever chan<- int)
		chan<- floor
	* pollElevButtons(reciever chan<- ButtonEvent)
		chan<- ButtonEvent
 io.go

src/network
 bcast.go
	* Transmitter(port int, chans ...interface{})
		// Transmits contents of the channel interface to port
	* Reciever(port int, chans ..interface{})
		// Recieves what is broadcasted on port, and writes to channel interface
	* checkArgs(chans ...interface{})
 localip.go
	- localIP string
	
	* LocalIP() (string, error)
		return localIP

 network.go

 peers.go
	& PeerUpdate struct{
	Peers	[]string
	New	string
	Lost	[]string
	}

	* Transmitter(port int, id string, transmitEnable <- chan bool)
	* Reciever(port int, peerUpdateCh<- PeerUpdate)
		

src/ordermanager
